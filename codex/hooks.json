{
  "PreToolUse": [
    {
      "matcher": "Bash",
      "hooks": [
        {
          "type": "command",
          "command": "jq -r '.tool_input.command' | grep -qE 'git push|gh pr create' && echo '{\"decision\":\"ask\",\"message\":\"push 전에 review 를 실행했나요? 인증·권한·시크릿·결제 변경이면 cso --diff 도.\"}' || echo '{\"decision\":\"allow\"}'",
          "timeout": 5,
          "statusMessage": "push/PR 사전 체크"
        }
      ]
    }
  ]
}
